VAVA LIVING — PHASE 01 ONLY: LOGIN FIRST-LOAD FIX

This phase is intentionally isolated. Do NOT work on any other VAVA requirement.

PATCH REPOSITORY:
https://github.com/mohamedamouseo-a11y/Vava-living-website-patches

PATCH FOLDER:
VAVA-Phase-01-Login-Fix/

TARGET PROJECT:
The VAVA Living WordPress project currently connected to the live website/GitHub module.

==================================================
WORKFLOW — IMPORTANT
==================================================

The website is now connected to GitHub and the USER will perform the Git push from inside the website/module AFTER you finish the code edits.

Therefore:
- Apply the Phase 01 changes to the CURRENT WEBSITE PROJECT WORKTREE only.
- DO NOT run git push.
- DO NOT merge anything.
- DO NOT create a pull request.
- DO NOT switch branches unless the current website workflow explicitly requires it.
- DO NOT modify production GitHub main yourself.
- Stop after code changes + local validation + git diff review.
- The USER will press Push from the website after reviewing your report.

Do NOT create backup files inside the tracked repository such as *.bak, *.backup, or copied theme files, because the user will push the website worktree afterward.
If a temporary backup is needed, place it OUTSIDE the repository or use git diff/git status for recovery.

==================================================
PHASE 01 GOAL
==================================================

Fix ONLY this problem:

The WordPress login page does not reliably log in on the first attempt and the user has to refresh the page first.

Required behavior:
- Open wp-login.php in a brand-new private/incognito session.
- Enter valid username/password.
- Click the normal WordPress Login button once.
- Dashboard opens immediately.
- NO refresh.
- NO press-and-hold verification.
- NO extra AJAX challenge before login.

Keep the VAVA visual login design.
Keep native WordPress username/password authentication.
Do not bypass authentication.

==================================================
ROOT CAUSE TO REMOVE
==================================================

The VAVA theme has a custom press-and-hold login guard in:

wp-content/themes/vava-living-theme-ar-v1/inc/admin-brand-vava.php
wp-content/themes/vava-living-theme-ar-v1/assets/js/login-vava.js

The old flow:
- renders a data-vava-login-hold control,
- creates a guard nonce,
- calls vava_login_hold_start,
- calls vava_login_hold_verify,
- hides/disables the normal submit button,
- requires vava_login_hold_token,
- rejects login through an authenticate filter when the token is absent/invalid.

This custom gate is RETIRED in Phase 01.

Do NOT repair the hold challenge.
Remove it from the active login path.

==================================================
STEP 1 — READ THE PATCH
==================================================

Read all files inside:
VAVA-Phase-01-Login-Fix/

Especially:
- wp-content/mu-plugins/vava-phase-01-login-fix.php
- patches/admin-brand-vava-login.patch
- patches/htaccess-login-no-cache.txt
- README.md

Before editing, inspect the CURRENT project files so you do not overwrite newer website changes.

==================================================
STEP 2 — INSTALL THE PHASE 01 MU PLUGIN
==================================================

Copy:

VAVA-Phase-01-Login-Fix/wp-content/mu-plugins/vava-phase-01-login-fix.php

into the website project at:

wp-content/mu-plugins/vava-phase-01-login-fix.php

Keep all existing V1/V2/V3 MU patches.
Do not delete or rename them.

The Phase 01 MU plugin is only a compatibility/safety layer for Login.

==================================================
STEP 3 — PATCH THE ACTIVE THEME LOGIN SOURCE
==================================================

Read:
VAVA-Phase-01-Login-Fix/patches/admin-brand-vava-login.patch

Apply the SAME SEMANTIC changes to:

wp-content/themes/vava-living-theme-ar-v1/inc/admin-brand-vava.php

Do not blindly overwrite the whole file.
Preserve newer unrelated code.

Final active behavior must be:

A) vava_admin_brand_login_assets() still loads the VAVA login CSS/branding.

B) It must NOT enqueue:
assets/js/login-vava.js

C) It must NOT localize/create the login guard object:
vavaLoginGuard

D) It must NOT register the hold markup on login_form:
vava_admin_brand_login_guard_markup

E) It must NOT register the custom authenticate blocker:
vava_admin_brand_validate_login_guard

F) Native WordPress username/password authentication remains untouched.

The old helper functions may remain defined if removing them creates unnecessary risk, but they must have NO active registration in the login flow.

==================================================
STEP 4 — LOGIN CACHE CONTROL
==================================================

Read:
VAVA-Phase-01-Login-Fix/patches/htaccess-login-no-cache.txt

If the project root contains an active .htaccess used by Apache/LiteSpeed, append the marked Phase 01 block ONCE.

Rules:
- Do not duplicate it.
- Do not replace WordPress rewrite rules.
- Do not modify unrelated cache rules.
- If .htaccess is not applicable to this project, skip it and report NOT APPLICABLE.

The MU plugin must still send PHP no-cache/no-store headers for wp-login.php.

==================================================
STEP 5 — STATIC / LOCAL VALIDATION
==================================================

Run, when binaries are available:

php -l wp-content/mu-plugins/vava-phase-01-login-fix.php
php -l wp-content/themes/vava-living-theme-ar-v1/inc/admin-brand-vava.php

git diff --check
git status --short
git diff -- wp-content/mu-plugins/vava-phase-01-login-fix.php wp-content/themes/vava-living-theme-ar-v1/inc/admin-brand-vava.php .htaccess

If PHP is NOT installed, report that honestly. Do NOT substitute brace counting and call it PHP lint.

Also search the ACTIVE website project for remaining ACTIVE registrations/references:

vava_admin_brand_login_guard_markup
vava_admin_brand_validate_login_guard
login-vava.js
vavaLoginGuard
vava_login_hold_token

Important distinction:
- Definitions or the unused JS source file may remain.
- Active enqueue/hook/filter/localization registrations must be removed.

==================================================
STEP 6 — OPTIONAL WEBSITE CHECK BEFORE USER PUSH
==================================================

If the current website environment immediately reflects edited files, request/read wp-login.php and verify:

- normal login form exists,
- normal submit button exists,
- login-vava.js is not loaded,
- data-vava-login-hold is absent,
- X-VAVA-Login-Fix: phase-01 header is present if MU plugin is active.

Do NOT use or expose real login credentials.

If the website does not reflect edits until the USER presses Push/Deploy, mark this check:
PENDING USER PUSH

This is expected and is NOT a failure of the code-edit phase.

==================================================
STRICT SCOPE
==================================================

DO NOT touch in Phase 01:
- Paths images
- Homepage images
- Digital products
- Full product description
- PDF uploads
- Session details layouts
- Questionnaires
- Booking capacity
- Policies
- Logo
- Tagline
- Meet/Zoom
- Any other VAVA requirement

==================================================
DO NOT PUSH
==================================================

At the end:
- DO NOT run git push.
- DO NOT merge.
- DO NOT modify another branch.
- Leave the edited files in the website worktree ready for the USER'S GitHub module Push action.

==================================================
FINAL REPORT
==================================================

Return a compact report containing:

1. Current git branch, if detectable.
2. Exact files added/modified.
3. php -l results, or exact reason PHP lint could not run.
4. git diff --check result.
5. Confirmation that the active press-and-hold registrations are removed.
6. Whether .htaccess Phase 01 block was applied or NOT APPLICABLE.
7. Website verification result, or PENDING USER PUSH.
8. git status --short output summary.
9. End with exactly:

No Git push was performed. Changes are ready for the user to push from the website.
