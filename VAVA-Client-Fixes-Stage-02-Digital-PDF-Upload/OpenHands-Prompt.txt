VAVA Living — Stage 02 Follow-up Hotfix #4
Protected PDF Progress — TARGET ACTUAL RENDERED NODE
LIVE HOSTING / FTP DEPLOYMENT

IMPORTANT
The PDF upload/replacement functionality works.
The remaining issue is VISUAL CSS LAYOUT ONLY.
The client has now provided a LIVE browser DOM inspection while the broken progress UI is visible.

LIVE FACT CONFIRMED BY BROWSER
The actual visible node is:

<div class="vava-upload-progress is-active" data-vava-upload-progress ...>

Its LIVE getBoundingClientRect() height was approximately:
25.6px

This is the key finding.
Patch 0005 attempted min-height:72px, but the live visible progress element still measured only ~25.6px.
Therefore the previous wrapper-dependent selector did NOT apply to the actual rendered node.

DO NOT:
- clone source repo
- create Git branch
- commit/push source
- work in /tmp
- change PHP
- change JS upload logic
- change upload limits/percentages/AJAX
- change PDF processing/storage/security
- change payments/checkout/access control
- modify unrelated admin styling

NEW APPROACH
Do NOT depend on .vava-protected-pdf-field, .vava-product-protected-file, or any guessed wrapper class to style the progress itself.

Target the REAL rendered node directly:

[data-vava-upload-progress].is-active:not([hidden])

Use :has(...) only to release height/overflow constraints on known generic parent containers that actually contain that real node.

PATCH TO APPLY
VAVA-Client-Fixes-Stage-02-Digital-PDF-Upload/patches/0006-target-real-progress-data-attribute.patch

TARGET LIVE FILE
wp-content/themes/vava-living-theme-ar-v1/assets/css/admin-selections.css

BACKUP FIRST
Create:
assets/css/admin-selections.css.bak-stage02-layout4

Do not overwrite any prior backup.

REQUIRED RESULT
The active progress panel must:
- be selected directly through [data-vava-upload-progress].is-active:not([hidden])
- display as grid
- remain in normal flow
- be centered horizontally
- use width:calc(100% - 28px)
- have height:auto
- have min-height:72px
- have max-height:none
- use margin approximately -6px auto 12px
- use padding 12px 16px
- use grid-template-rows:auto 8px
- use align-content:center
- show label and percentage in one row
- show an 8px full-width progress track below
- hide [data-upload-progress-meta] only when empty
- keep buttons and note below progress

PARENT CONTAINERS
While the real progress node is visible, release clipping/height restrictions using :has([data-vava-upload-progress].is-active:not([hidden])) on:
- .vava-selections-product-item
- .vava-repeater-item-body
- .vava-repeater-field
- .vava-media-field where applicable

Do not guess a product-specific wrapper class.

CRITICAL LIVE VALIDATION
Do NOT report success from static file inspection.
The client already proved that previous rules could exist in CSS without affecting the real node.

After deployment and hard refresh, start an actual PDF upload and run:

const progress = [...document.querySelectorAll('[data-vava-upload-progress]')].find(el => {
  const r = el.getBoundingClientRect();
  const s = getComputedStyle(el);
  return r.width > 0 && r.height > 0 && s.display !== 'none';
});

const cs = getComputedStyle(progress);
const r = progress.getBoundingClientRect();

console.log({
  className: progress.className,
  hidden: progress.hasAttribute('hidden'),
  display: cs.display,
  position: cs.position,
  width: cs.width,
  height: cs.height,
  minHeight: cs.minHeight,
  marginTop: cs.marginTop,
  paddingTop: cs.paddingTop,
  paddingBottom: cs.paddingBottom,
  rectTop: r.top,
  rectBottom: r.bottom,
  rectWidth: r.width,
  rectHeight: r.height
});

MANDATORY PASS CONDITIONS
While progress is visibly active:
- progress exists
- hidden === false
- display === grid
- rectHeight must be at least 65px (target ~72px or larger depending box model/content)
- minHeight computed style should reflect 72px
- full label visible
- percentage visible
- full progress track visible
- no content sits on/crosses the lower rounded border

If rectHeight is still around 25–30px:
STOP.
Do NOT claim success.
That means the new CSS still is not affecting the live rendered node.
Inspect DevTools > Styles for the actual [data-vava-upload-progress] element and report which rule/property is winning for min-height/height/display/padding.

Also inspect ancestor chain only if clipping remains AFTER the progress element itself measures >=65px.

REPORT
Return:
1. exact live file modified
2. backup created
3. whether 0006 applied directly or manually
4. active progress className
5. computed display
6. computed height
7. computed minHeight
8. rectHeight
9. marginTop
10. paddingTop/paddingBottom
11. whether label is fully visible
12. whether percentage is fully visible
13. whether progress track is fully visible
14. whether clipping remains
15. confirmation PDF upload still works

SUCCESS IS NOT ALLOWED if rectHeight remains around 25.6px.

STOP after this Stage 02 follow-up.
