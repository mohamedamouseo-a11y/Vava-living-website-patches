VAVA Living — Stage 02 Follow-up Hotfix #6
LIVE-SERVED admin-homepage.css verification

The PDF upload logic works. The remaining issue is CSS only.

CONFIRMED FROM THE CLIENT'S LIVE BROWSER:
A fresh no-cache fetch of the loaded admin-homepage.css returned:
- PATCH 0007 EXISTS: false
- NEW SELECTOR EXISTS: false
- OLD ABSOLUTE RULE EXISTS: true

Therefore the file copy edited in the previous FTP run is not the copy currently served to the browser, or the public asset is still being served from another origin/cache.

DO NOT change PHP/JS/upload logic.
DO NOT report success based only on an FTP file being edited.

PATCH:
VAVA-Client-Fixes-Stage-02-Digital-PDF-Upload/patches/0008-fix-live-served-admin-homepage-progress.patch

PUBLIC ASSET TO VERIFY:
https://thevavaliving.com/wp-content/themes/vava-living-theme-ar-v1/assets/css/admin-homepage.css

The currently served asset still contains the generic rule:
.vava-upload-progress{position:absolute;inset-inline:0;bottom:0;height:4px;background:transparent;overflow:hidden}

Find the actual WordPress/theme copy that serves this public asset. Check for duplicate site roots/theme copies on the FTP account instead of assuming the first wp-content path is live.

Before modifying the verified served file create:
admin-homepage.css.bak-stage02-layout6

Apply Patch 0008 ONLY to the verified served admin-homepage.css.
The patch contains a unique marker comment:
Stage 02 Follow-up #6 — LIVE-SERVED-ASSET marker

After editing, verify the public asset again with a cache-busting query string. The HTTP response itself must contain both:
- Stage 02 Follow-up #6 — LIVE-SERVED-ASSET marker
- [data-vava-upload-progress].is-active:not([hidden])

If the public response does not contain them, do not report success. Continue diagnosing the served document root/static cache until the exact served asset is identified, or report the unresolved mapping clearly.

The scoped protected-PDF override must result in:
- position: relative !important
- display: grid !important
- height: auto !important
- min-height: 72px !important
- overflow: visible !important
- full label, percentage and progress track visible

The old generic absolute/4px rule may remain for other media controls; the new scoped protected-PDF rule must appear later in the served CSS and win for the protected PDF element.

REPORT:
1. Candidate admin-homepage.css paths found.
2. Exact path proven to serve the public asset.
3. Exact file modified.
4. Backup created.
5. Whether Patch 0008 applied directly or manually.
6. Cache-busted public CSS URL checked.
7. Public marker present: true/false.
8. Public new selector present: true/false.
9. Any duplicate docroot/cache mismatch found.
10. Do not claim visual success unless the public response contains the marker.

STOP after this Stage 02 deployment-path fix.