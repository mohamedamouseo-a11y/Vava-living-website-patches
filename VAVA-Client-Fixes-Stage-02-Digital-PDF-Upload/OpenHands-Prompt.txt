VAVA Living — Stage 02 Follow-up Hotfix #11
Protected PDF Progress — DIRECT PERCENT SELECTOR WITHOUT MISSING WRAPPER
LIVE HOSTING / FTP DEPLOYMENT

CURRENT STATUS
The protected PDF upload works. The progress panel and track are visible. The remaining issue is ONLY the percentage text (11%, 88%, etc.) still sticking to the far-left edge.

LIVE DEVTOOLS PROOF
The client inspected all matched CSS rules for the real [data-upload-progress-percent] node.
Only ONE matching rule appeared:

.vava-upload-progress span {
  width: 0;
  height: 100%;
  background: var(--vava-admin-accent);
}

The Stage 02 #10 reset rule did NOT appear in matched rules at all.

Therefore this is NOT a specificity problem. The #10 selector itself does not match the real DOM.
The likely non-existent ancestor is:
.vava-selections-admin

Do NOT keep adding more padding.
Do NOT add more specificity to a selector that does not match.

DO NOT:
- modify PHP
- modify JavaScript
- modify upload logic/percentages
- modify AJAX/PDF processing
- modify outer panel geometry
- modify the already-correct track/head inset
- globally remove .vava-upload-progress span
- modify unrelated CSS

PATCH TO APPLY
VAVA-Client-Fixes-Stage-02-Digital-PDF-Upload/patches/0011-direct-percent-selector-without-missing-wrapper.patch

TARGET LIVE FILE
wp-content/themes/vava-living-theme-ar-v1/assets/css/admin-homepage.css

USE THE SAME LIVE-SERVED COPY
Use the exact admin-homepage.css already proven by the LIVE-SERVED-ASSET/public HTTP checks.

BACKUP FIRST
Create:
admin-homepage.css.bak-stage02-layout11

REQUIRED FIX
Add the direct selector WITHOUT .vava-selections-admin:

body.vava-advanced-page-editor [data-vava-upload-progress].is-active:not([hidden]) [data-upload-progress-percent]

with:

display:block!important;
width:auto!important;
min-width:max-content!important;
height:auto!important;
min-height:0!important;
max-height:none!important;
margin:0!important;
padding:0!important;
background:transparent!important;
overflow:visible!important;
box-sizing:border-box!important;
white-space:nowrap!important;

Do NOT change the existing working .vava-upload-progress-head or .vava-upload-progress-track inset rules.
Do NOT remove the generic .vava-upload-progress span rule globally.

PUBLIC CHECK
After deployment, fetch admin-homepage.css with a cache-busting query and confirm the response contains:
Stage 02 Follow-up #11 — direct percentage reset

LIVE MATCHED-RULE CHECK — MANDATORY
During an actual PDF upload, inspect the real percentage element:

const p = document.querySelector('[data-vava-upload-progress].is-active:not([hidden]) [data-upload-progress-percent]');

Then scan matched rules again.
The NEW #11 selector MUST appear in matched rules.
If it does not appear, DO NOT report success.

COMPUTED STYLE CHECK
During upload run:

const cs = getComputedStyle(p);
const r = p.getBoundingClientRect();
const head = p.closest('.vava-upload-progress-head');
const hr = head.getBoundingClientRect();
console.log({
  width: cs.width,
  height: cs.height,
  background: cs.backgroundColor,
  percentLeft: r.left,
  percentRight: r.right,
  headLeft: hr.left,
  headRight: hr.right,
  insideHead: r.left >= hr.left && r.right <= hr.right
});

MANDATORY PASS CONDITIONS
- the #11 direct selector appears in matched rules
- width !== '0px'
- background is transparent / not rgb(111,129,95)
- insideHead === true
- percentage visually aligns inside the same left inset as the progress track
- Arabic label and track remain unchanged
- PDF upload still works

REPORT
1. Exact live file modified.
2. Backup created.
3. Patch 0011 applied directly or manually.
4. Public #11 marker present true/false.
5. Does .vava-selections-admin exist as an ancestor of the real percentage node? true/false.
6. Does the #11 selector appear in matched rules? true/false.
7. Computed width.
8. Computed height.
9. Computed background.
10. percentLeft / percentRight.
11. headLeft / headRight.
12. insideHead true/false.
13. Visual result.
14. Confirmation upload still works.

SUCCESS IS NOT ALLOWED if width remains 0px or if only .vava-upload-progress span appears in matched rules.

STOP after this fix.