VAVA Living — Stage 02 Follow-up Hotfix #7
Protected PDF Progress — LEFT PADDING POLISH
LIVE HOSTING / FTP DEPLOYMENT

CURRENT STATUS
The major clipping/absolute-position issue is now visually fixed on production.
The protected PDF progress panel is visible and working.

The ONLY remaining request is a small visual adjustment:
add more INNER spacing on the LEFT side of the protected-PDF progress panel so the percentage/progress content does not sit too close to the left edge.

DO NOT:
- change PHP
- change JavaScript
- change upload behavior
- change percentages
- change AJAX/PDF processing
- change position/height/min-height/overflow logic that is now working
- change width or outer centering
- modify unrelated admin styling

PATCH TO APPLY
VAVA-Client-Fixes-Stage-02-Digital-PDF-Upload/patches/0009-add-left-padding-to-live-pdf-progress.patch

TARGET LIVE FILE
wp-content/themes/vava-living-theme-ar-v1/assets/css/admin-homepage.css

IMPORTANT
Use the SAME live-served admin-homepage.css copy that was proven in the previous step to contain the LIVE-SERVED-ASSET marker and the working protected-PDF override.
Do not edit another duplicate theme copy.

BACKUP FIRST
Create a new backup next to the verified live-served file:
admin-homepage.css.bak-stage02-layout7

REQUIRED CHANGE ONLY
For:
body.vava-advanced-page-editor .vava-selections-admin [data-vava-upload-progress].is-active:not([hidden])

set:
padding-left: 24px !important;
padding-right: 16px !important;

This should create a little more breathing room on the LEFT side while preserving the current right-side spacing.

DO NOT alter the current working values for:
- position: relative
- display: grid
- width
- max-width
- height
- min-height
- max-height
- margin
- grid rows
- overflow
- parent :has() rules

VALIDATION
After deployment and hard refresh:
1. Start a protected PDF upload.
2. Confirm the progress panel remains fully visible.
3. Confirm the percentage on the left has visibly more space from the left border.
4. Confirm the progress track is also inset consistently from the left.
5. Confirm right-side spacing remains unchanged.
6. Confirm the panel remains centered and does not become wider/narrower unexpectedly.
7. Confirm Replace/Delete buttons and the protected-file note stay unchanged.
8. Confirm PDF upload/replacement still works.

PUBLIC ASSET CHECK
Use a cache-busting request to the loaded admin-homepage.css and confirm the response contains:
Stage 02 Follow-up #7 — visual left inset

REPORT
1. Exact live file modified.
2. Backup created.
3. Whether Patch 0009 applied directly or manually.
4. Public marker present: true/false.
5. Left padding computed value.
6. Right padding computed value.
7. Visual result of percentage/track spacing.
8. Confirmation upload still works.

STOP after this visual polish.