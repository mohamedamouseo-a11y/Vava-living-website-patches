VAVA Living — Stage 02 Follow-up Hotfix #3
Protected PDF Progress — REAL DOM SELECTOR + POSITION FIX
LIVE HOSTING / FTP DEPLOYMENT

IMPORTANT
The PDF upload/replacement functionality already works.
The remaining issue is VISUAL CSS LAYOUT ONLY.
Previous reports said the issue was fixed, but the client confirmed it is STILL visible on production.
Do NOT mark this task complete based on static CSS inspection alone.
You MUST verify the real active DOM and actual element geometry while an upload is running.

DO NOT:
- clone the source repository,
- create Git branches,
- create commits in the source repository,
- push source changes to GitHub,
- work in /tmp,
- change PHP upload logic,
- change JavaScript upload percentages or AJAX logic,
- change PDF processing, storage, checkout, payments, permissions, or unrelated styling.

CONFIRMED REAL DOM / JS STATE
The live/source JavaScript adds the upload state directly to the actual protected PDF field:

field.addClass('is-uploading');

where field is selected by:

[data-vava-private-pdf-field]

The real element therefore becomes:

.vava-protected-pdf-field.is-uploading

IMPORTANT:
The previous manual hotfix report referenced:

.vava-product-protected-file.is-uploading

Do NOT rely on that selector unless you prove that class really exists in the LIVE DOM.
It is not the selector used by the actual upload JavaScript.

Also, the correct metadata selector is:

[data-upload-progress-meta]

NOT:

[v-data-upload-progress-meta]

CLIENT'S CURRENT VISUAL ISSUE
The progress/status panel is visually buried too low inside another rounded container.
The text and percentage can be seen near the lower border, while the progress content appears partially hidden.

Desired result:
- the inner progress panel stays INSIDE the protected PDF card,
- it is horizontally centered,
- it is raised slightly upward,
- the status text and percentage are vertically centered,
- the full progress track is visible,
- all content stays within the surrounding card borders,
- no clipping occurs.

PATCH TO APPLY
VAVA-Client-Fixes-Stage-02-Digital-PDF-Upload/patches/0005-fix-real-pdf-progress-selector-and-position.patch

TARGET LIVE FILE
wp-content/themes/vava-living-theme-ar-v1/assets/css/admin-selections.css

BACKUP
Before editing create:

assets/css/admin-selections.css.bak-stage02-layout3

Do not overwrite previous backups.

REQUIRED IMPLEMENTATION
Apply ONLY patch 0005 to the live CSS.
Keep 0003 and 0004 in place unless a directly conflicting rule must be overridden by the newer 0005 rules.
Do not remove unrelated CSS.

The new rules intentionally target the REAL state selectors:

.vava-protected-pdf-field.is-uploading
.vava-protected-pdf-field[data-processing-status="queued"]
.vava-protected-pdf-field[data-processing-status="processing"]
.vava-protected-pdf-field[data-processing-status="failed"]

The progress panel must:
- remain in normal flow,
- be width calc(100% - 28px),
- be centered using auto side margins,
- move upward slightly with margin-top around -8px,
- have enough internal height (about 72px minimum),
- keep status row and progress track visible,
- use a two-column status row: label + percent,
- use a full-width 8px progress track,
- hide only an EMPTY [data-upload-progress-meta] row,
- keep Replace/Delete buttons below the progress panel.

LIVE DOM VERIFICATION — MANDATORY
Do not claim success without performing these checks while an upload is ACTIVELY running.

1. Start replacing/uploading a protected PDF in the live WordPress admin.

2. Confirm the actual active field exists:

document.querySelectorAll('.vava-protected-pdf-field.is-uploading').length

Expected while network upload is active: at least 1.

3. Check whether the old selector from the previous report exists:

document.querySelectorAll('.vava-product-protected-file.is-uploading').length

Report the result. Do not assume it exists.

4. Obtain the active field and progress element:

const field = document.querySelector('.vava-protected-pdf-field.is-uploading') || document.querySelector('.vava-protected-pdf-field[data-processing-status="queued"], .vava-protected-pdf-field[data-processing-status="processing"], .vava-protected-pdf-field[data-processing-status="failed"]');
const progress = field && field.querySelector('[data-vava-upload-progress]');

5. Confirm the progress element is actually visible:
- hidden attribute must be absent while active,
- getComputedStyle(progress).display must not be 'none'.

6. Capture computed styles for the progress element:
- position
- width
- height
- marginTop
- overflow
- display
- alignContent

7. GEOMETRY CHECK — REQUIRED
Use getBoundingClientRect():

const fieldRect = field.getBoundingClientRect();
const progressRect = progress.getBoundingClientRect();

Verify:
- progressRect.top >= fieldRect.top
- progressRect.bottom <= fieldRect.bottom
- progressRect.left > fieldRect.left
- progressRect.right < fieldRect.right

The progress panel must physically fit inside the visible protected PDF field.

8. Inspect the status row and track:

const head = progress.querySelector('.vava-upload-progress-head');
const track = progress.querySelector('.vava-upload-progress-track');

Confirm both have non-zero width and height and both rectangles are fully inside progressRect.

9. Confirm these are simultaneously visible:
- upload status label
- percentage
- progress track/bar

10. Confirm the text is not touching or crossing the bottom border.

11. Confirm Replace/Delete buttons remain visually below the progress panel.

12. Confirm the protected-file note remains visible.

13. Confirm no horizontal overflow.

14. Hard refresh the admin page after deployment before final visual testing.

15. Test at desktop admin width. Test responsive/admin mobile width if available.

IMPORTANT SUCCESS CONDITION
Static checks such as "rule exists in admin-selections.css" are NOT sufficient.
The task is successful only if the actual LIVE DOM geometry confirms the visible progress panel fits inside its protected PDF field and the client-visible clipping is gone.

REPORT
Return:
1. Exact live file modified.
2. Backup created.
3. Whether 0005 applied directly or manually.
4. document.querySelectorAll('.vava-protected-pdf-field.is-uploading').length during upload.
5. document.querySelectorAll('.vava-product-protected-file.is-uploading').length during upload.
6. Computed progress display/position/width/height/marginTop.
7. fieldRect dimensions.
8. progressRect dimensions.
9. Whether progressRect is fully inside fieldRect on all four sides.
10. Whether head and track both have non-zero visible rectangles.
11. Desktop visual result.
12. Responsive result if available.
13. Confirmation PDF upload/replacement remains functional.

STOP after this hotfix.
Do not proceed to another client fix.
